import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'next-market-Admin',
  description: 'practice create next app',
};

const AdimnPage = () => {
  return <div>관리자만 볼 수 있는 페이지</div>;
};

export default AdimnPage;
